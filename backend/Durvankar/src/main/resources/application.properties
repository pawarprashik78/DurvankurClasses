# ─── Server ───────────────────────────────────────────────────────────────────
server.port=${PORT:8080}

# ─── MongoDB Atlas ─────────────────────────────────────────────────────────────
# URI is read programmatically in MongoConfig.java
# Set MONGODB_URI env var on Render (overrides the hardcoded fallback in MongoConfig)
app.mongodb.uri=${MONGODB_URI:mongodb+srv://pawarprashik78_db_user:P12345@cluster0.xpcoanz.mongodb.net/?appName=Cluster0}
app.mongodb.database=${MONGODB_DATABASE:durvankur_classes}

# ─── JWT ──────────────────────────────────────────────────────────────────────
app.jwt.secret=${JWT_SECRET:DurvankarClassesSecretKey2026ChangeThisInProduction}
app.jwt.expiration-ms=86400000

# ─── CORS ─────────────────────────────────────────────────────────────────────
# Not used in SecurityConfig (allowedOriginPatterns="*" is hardcoded)
# Set CORS_ORIGINS on Render if needed for stricter policy
app.cors.allowed-origins=${CORS_ORIGINS:*}

# ─── Logging ──────────────────────────────────────────────────────────────────
logging.level.org.mongodb.driver.cluster=WARN
logging.level.com.durvankarclasses=INFO
logging.level.org.springframework.security=WARN

# ─── Email (Gmail SMTP) ────────────────────────────────────────────────────────
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:pawarprashik78@gmail.com}
spring.mail.password=${MAIL_PASSWORD:gqlqbyxqvqsapunw}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.default-encoding=UTF-8

# ─── Cloudinary ────────────────────────────────────────────────────────────────
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dlmmxqmx2}
cloudinary.api-key=${CLOUDINARY_API_KEY:885911267327788}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:aXc7XpZmwEeOUZVn9l02N95z0i0}
